{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "npm install && npm run client",
    "client": "cd ../frontend && npm install -f && npm run build && cd ../backend && npm install -f && npm run start",
    "start": "node ./src/server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "luxon": "^3.5.0",
    "mongoose": "^8.12.0",
    "nodemon": "^3.1.9",
    "path": "^0.12.7",
    "url": "^0.11.4"
  }
}
